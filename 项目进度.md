# ESP-CLAW 移植项目进度

## 项目概述

将 [esp-claw](https://github.com/espressif/esp-claw)（乐鑫 AI Agent 框架）移植到**立创实战派 ESP32-S3 开发板**。

- **GitHub**: https://github.com/GBall5599/-ESP-CLAW
- **ESP-IDF**: v5.5.1
- **开发板**: ESP32-S3-WROOM-1-N16R8 (16MB Flash + 8MB PSRAM)
- **LLM 后端**: DeepSeek-v4-flash（OpenAI Compatible API）

---

## 已完成

### 阶段一：环境准备 + 例程适配

**状态: 已完成**

#### 1. 项目初始化
- [x] 拉取 esp-claw 上游源码到 `Ref/` 目录
- [x] 完成 esp-claw 项目完整解析文档 (`Ref/项目解析.md`)
- [x] 创建项目 README.md
- [x] 添加 .gitignore（排除 build/managed_components/sdkconfig 等生成文件）

#### 2. 开发环境配置
- [x] 安装 ESP-IDF v5.5.1（路径: `E:\ESPIDF\551\v5.5.1\esp-idf`）
- [x] 配置 VS Code ESP-IDF 插件（workspace + global settings.json）
- [x] 修复 ESP-IDF 插件芯片目标选择问题（state.vscdb 内部缓存）

#### 3. 例程 IDF v5.5.1 适配 (`14-handheld(new)/`)

原始例程基于 IDF v5.4.3，已完成以下迁移工作：

##### I2C 驱动迁移（核心改动）
旧版 I2C 驱动 (`driver/i2c.h`) 与新版 (`driver/i2c_master.h`) 不能在同一 I2C 端口混用。涉及改动：

- **`esp32_s3_szp.h`**: `#include "driver/i2c.h"` → `#include "driver/i2c_master.h"`
- **`esp32_s3_szp.c` - `bsp_i2c_init()`**: 用 `i2c_new_master_bus()` 创建总线，再用 `i2c_master_bus_add_device()` 为 QMI8658、PCA9557 创建设备句柄
- **QMI8658/PCA9557 读写**: `i2c_master_write_read_device()` / `i2c_master_write_to_device()` → `i2c_master_transmit_receive()` / `i2c_master_transmit()`（使用设备句柄）
- **触摸屏初始化**: 去掉 `(esp_lcd_i2c_bus_handle_t)` 类型转换，让 C11 `_Generic` 宏正确选择 v2 I2C 驱动；添加 `scl_speed_hz = 100000`
- **音频编解码器**: ES8311/ES7210 的 I2C 配置从 `.port = BSP_I2C_NUM` 改为 `.bus_handle = i2c_bus_handle`

##### sdkconfig.defaults 适配
- 中文注释替换为英文（IDF 的 kconfgen 使用 GBK 编码，无法读取 UTF-8 中文）
- 添加 `CONFIG_I2C_SKIP_LEGACY_CONFLICT_CHECK=y`（IDF 的 esp_lcd 组件同时编译 v1/v2 I2C 驱动，需跳过冲突检查）

##### 依赖清理
- 从 `idf_component.yml` 移除 `espressif/esp-sr: "~1.6.0"`（项目未使用语音识别功能，esp-sr 的传递依赖 esp-dsp 会触发 GCC 内部编译器错误 Segmentation Fault）

#### 4. 硬件测试结果（例程）

| 功能 | 状态 | 备注 |
|------|------|------|
| LCD 显示 (ST7789 320x240) | 正常 | |
| 触摸屏 (FT5x06) | 正常 | |
| 音频播放 (ES8311) | 正常 | 开机音乐可播放 |
| 音频录音 (ES7210) | 正常 | |
| WiFi 连接 | 正常 | |
| SD 卡读写 | 正常 | |
| 摄像头 (GC0308) | 正常 | |
| IMU (QMI8658) | 正常 | |
| 蓝牙 HID | 未验证 | 不知道怎么连接 |

---

### 阶段二：板级适配

**状态: 已完成**

#### 1. 工作目录准备
- [x] 将 `Ref/` 中的 esp-claw 复制到 `esp-claw/` 作为工作副本
- [x] 保留 `Ref/` 作为只读参考

#### 2. 板级定义文件创建 (`esp-claw/application/basic_demo/boards/esp32_s3_szp/`)

| 文件 | 状态 | 说明 |
|------|------|------|
| `board_info.yaml` | 已完成 | chip: esp32s3, board: esp32_s3_szp |
| `board_peripherals.yaml` | 已完成 | I2C/I2S/SPI/LEDC 四个总线 |
| `board_devices.yaml` | 已完成 | io_expander/audio_dac/audio_adc/display_lcd/lcd_touch 五个设备 |
| `sdkconfig.defaults.board` | 已完成 | 16MB Flash + 8MB Octal PSRAM + I2C 兼容 |
| `setup_device.c` | 已完成 | PCA9557 初始化 + ST7789 工厂 + FT5x06 工厂 |

#### 3. sdkconfig.defaults 板级配置
- [x] 添加 `CONFIG_ESP_BOARD_PERIPH_I2C/I2S/SPI/LEDC_SUPPORT=y`
- [x] 添加 `CONFIG_ESP_BOARD_DEV_AUDIO_CODEC/DISPLAY_LCD/LCD_TOUCH_I2C/CUSTOM_SUPPORT=y`
- [x] 添加 `CONFIG_ESP_BOARD_DEV_DISPLAY_LCD_SUB_SPI_SUPPORT=y`

#### 4. 关键设计决策
- PCA9557 作为 `type: custom` 设备，在 `setup_device.c` 中直接用 I2C API 操作（不需要完整的 esp_io_expander 抽象层）
- LCD 的 `cs_gpio_num: -1`，CS 由 PCA9557 扩展引脚控制
- I2C 地址使用 8-bit 格式（ES8311=0x30, ES7210=0x82, FT5x06=0x38）

---

### 阶段三：编译调通

**状态: 已完成**

- [x] `idf.py gen-bmgr-config` 自动生成 `components/gen_bmgr_codes/`
- [x] `idf.py build` 编译通过（固件大小 ~2.5MB，分区利用率 59%）
- [x] 固件可正常烧录和启动

#### 编译过程中解决的问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| gen_bmgr_codes 头文件找不到 | sdkconfig 中 `CONFIG_ESP_BOARD_*` 选项未启用 | 在 sdkconfig.defaults 中添加 9 个 CONFIG 选项 |
| cJSON 类型错误 | `cJSON_CreateString()` 返回 `cJSON*` 被赋给 `char*` | 修正类型为 `cJSON *escaped` |
| Flash 大小被重置为 2MB | sdkconfig 重新生成时丢失 | 确认 sdkconfig.defaults 中有 `CONFIG_ESPTOOLPY_FLASHSIZE_16MB=y` |

---

### 阶段四：运行时验证

**状态: 部分完成**

#### 硬件初始化验证（全部通过）

```
PERIPH_I2C: I2C master bus initialized successfully
PERIPH_I2S: I2S[1] STD TX/RX initialize success
PERIPH_SPI: SPI-2 peripheral initialized successfully
PERIPH_LEDC: LEDC initialized: gpio=42, freq=5000Hz
SZP_SETUP_DEVICE: PCA9557 initialized: LCD_CS=LOW, PA_EN=HIGH
DEV_AUDIO_CODEC: Successfully initialized codec: audio_dac (es8311)
DEV_AUDIO_CODEC: Successfully initialized codec: audio_adc (es7210)
DEV_DISPLAY_LCD: Successfully initialized LCD display: display_lcd (st7789, spi)
DEV_LCD_TOUCH_I2C: Successfully initialized: lcd_touch (ft5x06)
BOARD_MANAGER: Board manager initialized
```

#### WiFi 网络功能（已验证）
- [x] APSTA 模式（STA 连接路由器 + AP 热点供配网）
- [x] 自动连接已保存的 WiFi（SSID: ccccc2.4g）
- [x] Web 配网门户（http://192.168.4.1）
- [x] STA IP: 192.168.0.105
- [x] SNTP 时间同步正常

#### LLM Agent 功能（已验证）
- [x] DeepSeek-v4-flash 通过 Qwen Compatible 配置文件接入
- [x] 串口控制台 `ask` 命令（仅支持英文，中文会乱码）
- [x] 网页聊天界面 `http://192.168.0.105/chat`（中英文均正常）
- [x] 33 个能力（caps）注册成功，5 个 LLM 可见能力组
- [x] 会话历史上下文正常传递

#### IM 通道状态

| IM 通道 | 状态 | 备注 |
|---------|------|------|
| Telegram | 已配置但被墙 | api.telegram.org 在国内无法访问，已清除 token |
| QQ | 未配置 | 需要配置凭据 |
| 飞书 | 未配置 | 需要 Webhook 公网回调地址 |
| 微信 | 未配置 | 需要配置凭据 |
| **网页聊天** | **已验证可用** | http://<设备IP>/chat，中英文支持 |

#### MCP 功能
- [x] MCP Server 启动: http://esp-claw.local:18791/mcp_server
- [x] mDNS 正常工作

---

## 待完成 / 可选优化

### 功能增强
- [ ] 摄像头 (GC0308) 在 esp-claw 中的板级定义和验证
- [ ] SD 卡 (SDMMC 1-bit) 在 esp-claw 中的板级定义
- [ ] IMU (QMI8658) 在 esp-claw 中的支持
- [ ] 音频输入/输出（TTS/STT）在 Agent 中的集成
- [ ] 蓝牙 BLE HID 功能验证

### IM 通道（国内可用）
- [ ] 微信公众号/小程序接入（需要公众号开发权限）
- [ ] 钉钉机器人接入（类似飞书 Webhook）
- [ ] 自建 Telegram 代理或使用国内 LLM API 替代方案

### 用户体验
- [ ] Web 聊天页面增加 Markdown 渲染（LLM 返回的表格/代码块）
- [ ] LCD 显示 Agent 状态/对话内容
- [ ] 触摸屏交互界面
- [ ] 语音唤醒和对话

---

## 项目后续发展方向

### 方向一：硬件扩展
- 摄像头 (GC0308) 板级定义与 Agent 视觉能力集成
- SD 卡 (SDMMC 1-bit) 文件持久化支持
- IMU (QMI8658) 运动感知与 Agent 联动
- 音频 TTS/STT 实现语音对话

### 方向二：交互体验优化
- LCD 显示 Agent 状态、对话内容、表情动画
- 触摸屏交互界面（虚拟键盘、设置面板）
- Web 聊天页面增加 Markdown 渲染（表格/代码块）
- 语音唤醒词 + 实时对话

### 方向三：国内 IM 通道打通
- 微信公众号/小程序接入（需公众号开发权限）
- 钉钉机器人接入（类似飞书 Webhook）
- Web 桥接方案（通过云服务中转消息）

### 方向四：Agent 能力深化
- Lua 硬件操控脚本（GPIO/I2C/SPI/摄像头/显示屏）
- MCP 工具链完善（更多内置工具）
- 定时任务调度（闹钟、定时提醒、定时采集）
- 记忆系统优化（长期记忆 + 档案管理）

### 方向五：系统稳定性与部署
- OTA 空中固件升级
- 看门狗与异常恢复机制
- 功耗优化（Light Sleep / Deep Sleep）
- 多板适配框架（支持更多 ESP32 开发板）

---

## 踩坑记录

### 1. VS Code ESP-IDF 插件配置

插件存储配置在三个层级，全部需要正确配置才能工作：
- **workspace settings**: `.vscode/settings.json`（项目级）
- **global settings**: `AppData/Roaming/Code/User/settings.json`（用户级）
- **内部缓存**: `AppData/Roaming/Code/User/workspaceStorage/<hash>/state.vscdb`（SQLite 数据库）

其中 state.vscdb 的 `idf.currentSetup` 字段会缓存旧路径，导致芯片目标选择功能异常。需要用 sqlite3 直接修改数据库才能修复。

### 2. sdkconfig.defaults 编码问题

IDF 的 Python 工具链（kconfgen）在中文 Windows 上使用 GBK 编码读取配置文件。UTF-8 中文注释会导致 `UnicodeDecodeError: 'gbk' codec can't decode byte`。解决方案：注释全部使用英文。

### 3. I2C 新旧驱动冲突

ESP-IDF v5.5.x 中，旧 I2C 驱动 (`driver/i2c.h`) 和新 I2C 驱动 (`driver/i2c_master.h`) 不能在同一端口混用。旧驱动源码中有 `__attribute__((constructor))` 函数，在程序启动时自动检测并 abort。

此外，IDF 的 `esp_lcd` 组件总是同时编译 v1（旧 API）和 v2（新 API）两个 LCD I2C 面板驱动。即使只用 v2，v1 也会把旧 I2C 驱动链入二进制。需要设置 `CONFIG_I2C_SKIP_LEGACY_CONFLICT_CHECK=y` 跳过检查。

### 4. _Generic 宏类型匹配

`esp_lcd_new_panel_io_i2c()` 在 C 中用 `_Generic` 宏根据 bus 参数类型选择 v1 或 v2。如果将 `i2c_master_bus_handle_t`（指针类型）强制转换为 `esp_lcd_i2c_bus_handle_t`（= `uint32_t`），`_Generic` 会匹配到 `default` 分支即 v1（旧 API），导致运行时 I2C 冲突。正确做法是直接传 `i2c_master_bus_handle_t` 类型的变量。

### 5. esp-dsp GCC 内部编译器错误

`espressif/esp-sr` 组件的传递依赖 `esp-dsp` 在编译 `dspi_conv_f32_ansi.c` 时触发 GCC ICE（Internal Compiler Error: Segmentation Fault）。esp-sr 在本项目中未使用，移除后编译通过。

### 6. gen_bmgr_codes CONFIG 选项丢失

`idf.py gen-bmgr-config` 生成的代码引用了 `esp_board_manager` 设备/外设子目录的头文件（如 `dev_audio_codec.h`），但这些头文件的路径只在对应的 `CONFIG_ESP_BOARD_*_SUPPORT` 选项启用时才会被加入 INCLUDE_DIRS。如果 sdkconfig 被重新生成导致这些选项丢失，编译会报 "No such file or directory"。

解决方案：在 `sdkconfig.defaults` 中显式声明所有需要的 CONFIG 选项。

### 7. HTTP POST 处理中的 cJSON 悬空指针

`chat_ask_handler()` 中通过 `cJSON_GetObjectItemCaseSensitive()` 获取的字符串指针在 `cJSON_Delete()` 后变成悬空指针，导致发给 LLM 的中文消息变成乱码。

解决方案：用 `strdup()` 复制字符串，在 `claw_core_submit()` 后 `free()` 释放副本。

### 8. Telegram API 被墙

Telegram Bot API (`api.telegram.org`) 在中国大陆无法直接访问，导致 `esp-tls: Failed to open new connection` 循环刷错误日志。清除 NVS 中的 tg_bot_token 后恢复正常。

### 9. claw_core 超时参数双重单位转换

`claw_core_submit()` 和 `claw_core_receive_for()` 的 timeout 参数已经是**毫秒**单位，但代码中误用了 `pdMS_TO_TICKS()` 做了一次转换（将 ms → ticks），函数内部又做了一次转换（ticks → ticks），导致实际超时时间被缩短约 10 倍。

例如 `pdMS_TO_TICKS(120000)` 在 1000Hz tick 下变成 120000 ticks，函数内部再 `pdMS_TO_TICKS(120000)` = 120000 ticks = 120 秒。但实际上 FreeRTOS tick rate 不是 1000Hz 而是 100Hz，所以 120000 ms 被转成 12000 ticks，函数内部再按 ms 处理时实际变成了 `pdMS_TO_TICKS(12000)` = 1200 ticks = 12 秒。

解决方案：直接传毫秒值，不做 `pdMS_TO_TICKS` 转换。

---

## Git 提交历史

```
921ba02 fix: migrate I2C to new driver API for IDF v5.5.1 compatibility
dec9968 feat: 添加 14-handheld 例程和 IDF v5.5.1 适配版本
7a67f6c fix: 将 Ref 改为普通文件夹，配置 IDF v5.5.1 路径
beb1e28 init: 初始化项目，添加 esp-claw 子模块和项目解析文档
```
